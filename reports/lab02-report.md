# Лабораторна робота №02 — звіт про фактичний стан

**Тема:** Налаштування середовища розробки та Git workflow.
**Дата перевірки:** 21 вересня 2026 року.
**Власник репозиторію:** GitHub `istdjfgh`.
**Репозиторій:** https://github.com/istdjfgh/python-utils
**Запрошений учасник:** `xx1x1x2x`.
**Статус:** робочий звіт; командні вимоги ще не завершені.

ПІБ і навчальну групу власник не надав. Вони не підміняються вигаданими
даними. Особисті висновки та підтвердження виконання автор додає самостійно.

## Мета

Опанувати базові операції Git, організацію гілок та Pull Request,
взаємне review, вирішення конфліктів і підготовку релізу.

## Походження матеріалів

Код отримано з архіву `python-utils-lab02.zip`, наданого користувачем.
Публікацію та оформлення підготовлено з допомогою ШІ.
Імпорт не представляється як самостійно написані модулі чотирьох учасників.

## Перевірений результат

- Встановлено Git `2.52.0.windows.1`.
- Створено публічний репозиторій `istdjfgh/python-utils`.
- Для `xx1x1x2x` надіслано запрошення з правом внесення змін.
- У коді є калькулятор, конвертер, генератор паролів і статистика тексту.
- Усі 7 вихідних модульних тестів успішно пройшли локальну перевірку.
- Головне меню запускається; пункт `0` завершує програму.
- README містить інструкції запуску, `.gitignore` виключає кеші Python,
  віртуальні середовища, `.env` і файли IDE.

## Відтворення перевірки

```powershell
git clone https://github.com/istdjfgh/python-utils.git
cd python-utils
python -m unittest discover -v
python main.py
```

Вихідні тести охоплюють додавання й ділення, ділення на нуль,
конвертацію довжини та температури, довжину пароля та статистику тексту.
Це базова, а не вичерпна перевірка всіх граничних випадків.

## Опубліковані матеріали GitHub

- Початковий коміт: `d278780` — код, документація і звіт.
- [Issues](https://github.com/istdjfgh/python-utils/issues) №1–4 містять
  додаткові тести, командне review, навчальний конфлікт і підготовку до здачі.
- [PR №5](https://github.com/istdjfgh/python-utils/pull/5) пов'язаний
  з Issue №1 через `Closes #1` та додає 13 тестів.
- У гілці `codex/edge-case-tests` усі 20 тестів пройшли.
  PR відкритий, незалежного схвалення ще немає; у `main` залишаються 7 тестів.
- Запрошення `xx1x1x2x` на момент перевірки має статус pending.
  Формально призначити його рецензентом GitHub поки не дозволив.
- [Реліз v1.0.0](https://github.com/istdjfgh/python-utils/releases/tag/v1.0.0)
  призначений для початкового робочого коду з `main`, а не як підтвердження
  завершення всіх навчальних вимог.

Розширену перевірку можна відтворити після клонування:

```powershell
git switch --track origin/codex/edge-case-tests
python -m unittest discover -v
git switch main
```

## Що потребує участі команди

1. Прийняття запрошення `xx1x1x2x` і погодження ролей.
2. Реальна робота кожного учасника у власних гілках.
3. Pull Request зі змістовними коментарями та review іншої людини.
4. Навчальний конфлікт із його вирішенням у VS Code.
5. Власні звіти, висновки й скріншоти учасників.
6. Доповнення складу до 3–4 осіб або погодження викладачем роботи удвох.

2FA та SSH-доступ не перевірялися; їх налаштування не заявляється як виконане.
Наявність готового коду не підтверджує автоматично виконання рівнів 2 і 3.

## Moodle

Під час перевірки курсу доступне лише завдання «Лабораторна робота 01»,
у якому вже подано `lab01-report-2.md`. Цю відповідь не змінено.
Матеріали №02 ще не подано: окреме завдання для нього не відображалося.
